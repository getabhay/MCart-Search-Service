apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mcart-product-search-service-ingress
  namespace: mcart
spec:
  ingressClassName: traefik
  rules:
    - host: search.<replace-with-your-domain>
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mcart-product-search-service
                port:
                  number: 8081